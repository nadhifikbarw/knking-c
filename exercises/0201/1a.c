#include <stdio.h>

// 1a.c is the original program given by the book to be compiled to observe
// whether warning will be emitted by compiler with missing return/exit()
int main(void) {
    printf("hello, world\n");
}
